# Pipeline secrets. Mirrored as GitHub Actions secrets for the scheduled run.
# Kept out of the repo-root .env because Next.js loads that one, and the
# service-role key bypasses RLS.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
